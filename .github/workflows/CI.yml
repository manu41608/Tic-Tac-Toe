name: Test-CI

on:
  push:
    branches:
      - feature

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Pages
        run: echo "hello"
